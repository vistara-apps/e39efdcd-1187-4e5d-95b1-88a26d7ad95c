import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'PayNoti - Never miss a payment',
  description: 'Track everything, pay smarter. Smart payment notification and aggregation app for Web3 users.',
  keywords: ['payments', 'notifications', 'web3', 'base', 'crypto'],
  authors: [{ name: 'PayNoti Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
